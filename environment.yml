name: yflow
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip

  # Deep learning
  - pytorch
  - torchvision
  - pytorch-cuda=12.4

  # Numerics / data
  - numpy
  - scipy
  - scikit-learn
  - pandas

  # Visualization
  - matplotlib
  - seaborn
  - pillow
  - tqdm

  # Config / notebooks
  - pyyaml
  - jupyter
  - ipykernel

  - pip:
      - triton==2.3.0
      - wandb
      - einops
      - omegaconf
      - tensorboard
      - torchdiffeq
      - huggingface_hub
      - diffusers>=0.33.0
      - accelerate
      - transformers
      - sentencepiece
      - av                    # CLEVRER MP4 디코딩 (PyAV)
      - gdown                 # CLEVRER PropNet Google Drive 아카이브
